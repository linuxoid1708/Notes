
# Xfreerdp 

```bash
xfreerdp /u:<user> /p:'<pass>' /d:<domain> /v:<IP> /dynamic-resolution /drive:.,linux /bpp:8 /compression -themes -wallpaper /clipboard /audio-mode:0 /auto-reconnect -glyph-cache
```
